#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a = 5;
    int *p = &a;
    cout << *p << endl; // The dereference operator * is used to access the value stored at the address the pointer is pointing to.
}